<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.csseproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CSSEProject">
        <activity android:name=".ProfileView.LoginPage"></activity>
        <activity android:name=".ProfileView.ProfilePageEditting" />
        <activity android:name=".ProfileView.ProfilePage" />
        <activity android:name=".ProfileView.informationSubmissionForm" />
        <activity android:name=".ProfileView.ProfileView" />
        <activity android:name=".OrderDetails.OrderStatus" />
        <activity android:name=".HomePage.OrderForm" />
        <activity android:name=".IndexPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>